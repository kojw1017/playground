global:
  scrape_interval: 30s # 5초마다 데이터 긁어가기

scrape_configs:
  - job_name: 'spring-boot-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      # host.docker.internal은 컨테이너 안에서 내 PC(스프링)를 찾게 해줍니다.
      - targets: ['host.docker.internal:8080']